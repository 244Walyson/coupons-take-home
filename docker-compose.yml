version: "3.8"

services:
  coupons-app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    networks:
      - coupons-network

networks:
  coupons-network:
    driver: bridge
